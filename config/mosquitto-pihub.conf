# Pi Hub - MQTT 安全边界
# 依据 PI_HUB_SPEC.md §1：Mosquitto 只绑本机，不对外暴露。
# Flask、监控线程、企微 Bot 都在本机，无需局域网监听。
# 未来若有别的设备要接 MQTT，再改为局域网监听并加 password_file。
#
# 安装位置：/etc/mosquitto/conf.d/pihub.conf

listener 1883 127.0.0.1
allow_anonymous true
